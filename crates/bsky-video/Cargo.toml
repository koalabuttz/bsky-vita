[package]
name = "bsky-video"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "sceAvPlayer + sceAudioOut wrappers for Bluesky video embeds"

[lib]
path = "src/lib.rs"

[dependencies]
vitasdk-sys = { workspace = true }
bsky-log    = { path = "../bsky-log" }

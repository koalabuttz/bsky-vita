[package]
name = "bsky-models"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AT Protocol type re-exports — thin wrapper over atrium-api"

[dependencies]
atrium-api = { workspace = true }

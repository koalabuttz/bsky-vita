[package]
name = "bsky-net"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "XRPC client over ureq + rustls; atrium HttpClient implementation"

[dependencies]
atrium-xrpc = { workspace = true }
ureq = { workspace = true }

[dev-dependencies]
tempfile = "3"
